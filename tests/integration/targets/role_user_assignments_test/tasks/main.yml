---
- name: Generate a test ID
  ansible.builtin.set_fact:
    test_id: "{{ lookup('password', '/dev/null chars=ascii_letters length=16') }}"
  when: test_id is not defined

- name: Preset vars
  ansible.builtin.set_fact:
    username: "GW-Collection-Test-RoleUserAssignments-{{ test_id }}"
    organization_name: "GW-Collection-Test-Organization-{{ test_id }}"
    name_prefix: "GW-Collection-Test-Team-{{ test_id }}"
    custom_role_name: "GW-Custom-RoleUser-{{ test_id }}"
    custom_team_role_name: "GW-Custom-RoleUser-Team-{{ test_id }}"

- name: Run Tests
  module_defaults:
    group/ansible.platform.gateway:
      gateway_hostname: "{{ gateway_hostname }}"
      gateway_username: "{{ gateway_username }}"
      gateway_password: "{{ gateway_password }}"
      gateway_validate_certs: "{{ gateway_validate_certs | bool }}"

  block:
    # <Users> -------------------
    - name: Create Users
      ansible.platform.user:
        username: "{{ username }}--User-1"
      register: user

    - name: Assert a creation changes the system
      ansible.builtin.assert:
        that:
          - user is changed

    - name: Create User2
      ansible.platform.user:
        username: "{{ username }}--User-2"
      register: user2

    - name: Assert a creation changes the system
      ansible.builtin.assert:
        that:
          - user2 is changed

    - name: Create User3
      ansible.platform.user:
        username: "{{ username }}--User-3"
      register: user3

    - name: Assert a creation changes the system
      ansible.builtin.assert:
        that:
          - user3 is changed

    - name: Create User4
      ansible.platform.user:
        username: "{{ username }}--User-4"
      register: user4

    - name: Assert a creation changes the system
      ansible.builtin.assert:
        that:
          - user4 is changed

    # <Organizations> -------------------
    - name: Create Organization 1
      ansible.platform.organization:
        name: "{{ organization_name }}"
      register: org

    - name: Assert a creation changes the system
      ansible.builtin.assert:
        that:
          - org is changed

    - name: Create Organization 2
      ansible.platform.organization:
        name: "{{ organization_name }}-2"
      register: org2

    - name: Assert a creation changes the system
      ansible.builtin.assert:
        that:
          - org2 is changed
    # </Organizations> -------------------

    # <Teams> -------------------
    - name: Create Team 1
      ansible.platform.team:
        name: "{{ name_prefix }}-Team-1"
        organization: "{{ org.name }}" # Org by name
        description: Team 1
      register: team1

    - name: Assert that the creation changed the system
      ansible.builtin.assert:
        that:
          - team1 is changed

    - name: Create Team 2
      ansible.platform.team:
        name: "{{ name_prefix }}-Team-2"
        organization: "{{ org2.name }}" # Org by name
        description: Team 2
      register: team2

    - name: Assert that the creation changed the system
      ansible.builtin.assert:
        that:
          - team2 is changed
    # </Teams> -------------------

    - name: Fetch Ansible ID for team2 & organization 2
      ansible.builtin.set_fact:
        team2_ansible_id: "{{ query('ansible.platform.gateway_api', 'teams/' + (team2.id | string),
         **connection_info)[0].summary_fields.resource.ansible_id }}"
        org2_ansible_id: "{{ query('ansible.platform.gateway_api', 'organizations/' + (org2.id | string),
         **connection_info)[0].summary_fields.resource.ansible_id }}"

    # # <Role User Assignments> -------------------
    - name: Assign Admins by Role User Assignments
      ansible.platform.role_user_assignment: &org_assignment
        object_ids: ["{{ org.id }}"]
        role_definition: Organization Admin
        user: "{{ user2.id }}"
      register: org_admin_role_assignment

    - name: Assert that adding user as org admin worked
      ansible.builtin.assert:
        that:
          - org_admin_role_assignment is changed

    # Multiple assignments
    - name: Assign Admins by Role User Assignments (idempotent check)
      ansible.platform.role_user_assignment: *org_assignment
      register: org_admin_role_assignment_check

    - name: Assert that org_admin_role_assignment_check is not changed
      ansible.builtin.assert:
        that:
          - org_admin_role_assignment_check is not changed

    - name: Assign Organization Admin by Role User Assignments
      ansible.platform.role_user_assignment: &orgs_assignment
        object_ids: ["{{ org.id }}", "{{ organization_name }}-2"]
        role_definition: Organization Admin
        user: "{{ user3.id }}"
      register: org_admin_role_assignment2

    - name: Assert that adding user as org admin worked
      ansible.builtin.assert:
        that:
          - org_admin_role_assignment2 is changed

    - name: Assign Organization Admin by Role User Assignments (idempotent check)
      ansible.platform.role_user_assignment: *orgs_assignment
      register: org_admin_role_assignment2_check

    - name: Assert that org_admin_role_assignment2_check is not changed
      ansible.builtin.assert:
        that:
          - org_admin_role_assignment2_check is not changed

    - name: Assign Organization Admin by Role User Assignments with object_ansible_id
      ansible.platform.role_user_assignment: &orgs_assignment3
        object_ansible_id: "{{ org2_ansible_id }}"
        role_definition: Organization Admin
        user: "{{ user.id }}"
      register: org_admin_role_assignment3

    - name: Assert that adding user as org admin worked for org2
      ansible.builtin.assert:
        that:
          - org_admin_role_assignment3 is changed

    - name: Assign Organization Admin by Role User Assignments (idempotent check)
      ansible.platform.role_user_assignment: *orgs_assignment3
      register: org_admin_role_assignment3_check

    - name: Assert that org_admin_role_assignment3_check is not changed
      ansible.builtin.assert:
        that:
          - org_admin_role_assignment3_check is not changed

    - name: Assign Team Admin by Role User Assignments
      ansible.platform.role_user_assignment: &team_assignment
        object_ids: ["{{ team1.id }}", "{{ name_prefix }}-Team-2"]
        role_definition: Team Admin
        user: "{{ user2.id }}"
      register: team_admin_role_assignment

    - name: Assert that adding user as team admin worked
      ansible.builtin.assert:
        that:
          - team_admin_role_assignment is changed

    - name: Assign Team Admin by Role User Assignments (idempotent check)
      ansible.platform.role_user_assignment: *team_assignment
      register: team_admin_role_assignment_check

    - name: Assert that team_admin_role_assignment_check is not changed
      ansible.builtin.assert:
        that:
          - team_admin_role_assignment_check is not changed

    - name: Assign Team Admin by Role User Assignments for team2 with user4
      ansible.platform.role_user_assignment: &team2_admin_assignment
        object_ansible_id: "{{ team2_ansible_id }}"
        role_definition: Team Admin
        user: "{{ user4.id }}"
        state: present
      register: team2_admin_role_assignment

    - name: Assert that adding user as team2 admin worked
      ansible.builtin.assert:
        that:
          - team2_admin_role_assignment is changed

    - name: Assign Team Admin by Role User Assignments for team2 with user4 (idempotent check)
      ansible.platform.role_user_assignment: *team2_admin_assignment
      register: team2_admin_role_assignment_check

    - name: Assert that team2_admin_role_assignment_check is not changed
      ansible.builtin.assert:
        that:
          - team2_admin_role_assignment_check is not changed

    - name: Create custom org-scoped role (shared.organization)
      ansible.platform.role_definition:
        name: "{{ custom_role_name }}"
        description: "Custom role for role_user_assignment content_type resolution test (organization)"
        content_type: "shared.organization"
        permissions:
          - "shared.view_organization"
        state: present
      register: custom_role

    - name: Assert custom org role definition was created
      ansible.builtin.assert:
        that:
          - custom_role is changed

    - name: Assign custom role to user on organization (object_ids by id)
      ansible.platform.role_user_assignment: &custom_user_org_assignment
        role_definition: "{{ custom_role_name }}"
        user: "{{ user4.id }}"
        object_ids:
          - "{{ org.id }}"
        state: present
      register: custom_user_role_assignment

    - name: Assert custom role user assignment changed the system
      ansible.builtin.assert:
        that:
          - custom_user_role_assignment is changed

    - name: Assign custom role to user on organization (idempotent check)
      ansible.platform.role_user_assignment: *custom_user_org_assignment
      register: custom_user_role_assignment_idem

    - name: Assert custom role user assignment idempotent re-run is unchanged
      ansible.builtin.assert:
        that:
          - custom_user_role_assignment_idem is not changed

    - name: Query API for custom role user assignment
      ansible.builtin.uri:
        url: "{{ gateway_hostname }}api/gateway/v1/role_user_assignments/?role_definition={{ custom_role.id }}&user={{ user4.id }}"
        user: "{{ gateway_username }}"
        password: "{{ gateway_password }}"
        force_basic_auth: true
        validate_certs: "{{ gateway_validate_certs | bool }}"
        return_content: true
      register: custom_user_assignment_query

    - name: Assert custom role user assignment exists in API
      ansible.builtin.assert:
        that:
          - custom_user_assignment_query.json.count | int > 0
        fail_msg: >-
          No role_user_assignment found for custom role id {{ custom_role.id }} and user id {{ user4.id }}.

    - name: Create custom team-scoped role (shared.team)
      ansible.platform.role_definition:
        name: "{{ custom_team_role_name }}"
        description: "Custom role for role_user_assignment content_type resolution test (team)"
        content_type: "shared.team"
        permissions:
          - "shared.view_team"
        state: present
      register: custom_team_role
      failed_when: >-
        custom_team_role is failed and
        'Creating custom roles for teams is disabled' not in custom_team_role.msg | default('')

    - name: Assert custom team role definition was created
      ansible.builtin.assert:
        that:
          - custom_team_role is changed
      when: custom_team_role is defined and custom_team_role.id is defined

    - name: Assign custom team role to user (object_ids by id)
      ansible.platform.role_user_assignment: &custom_user_team_assignment
        role_definition: "{{ custom_team_role_name }}"
        user: "{{ user.id }}"
        object_ids:
          - "{{ team1.id }}"
        state: present
      register: custom_team_user_role_assignment
      when: custom_team_role is defined and custom_team_role.id is defined

    - name: Assert custom team role user assignment changed the system
      ansible.builtin.assert:
        that:
          - custom_team_user_role_assignment is changed
      when: custom_team_role is defined and custom_team_role.id is defined

    - name: Assign custom team role to user (idempotent check)
      ansible.platform.role_user_assignment: *custom_user_team_assignment
      register: custom_team_user_role_assignment_idem
      when: custom_team_role is defined and custom_team_role.id is defined

    - name: Assert custom team role user assignment idempotent re-run is unchanged
      ansible.builtin.assert:
        that:
          - custom_team_user_role_assignment_idem is not changed
      when: custom_team_role is defined and custom_team_role.id is defined

    - name: Query API for custom team role user assignment
      ansible.builtin.uri:
        url: "{{ gateway_hostname }}api/gateway/v1/role_user_assignments/?role_definition={{ custom_team_role.id }}&user={{ user.id }}"
        user: "{{ gateway_username }}"
        password: "{{ gateway_password }}"
        force_basic_auth: true
        validate_certs: "{{ gateway_validate_certs | bool }}"
        return_content: true
      register: custom_team_user_assignment_query
      when: custom_team_role is defined and custom_team_role.id is defined

    - name: Assert custom team role user assignment exists in API
      ansible.builtin.assert:
        that:
          - custom_team_user_assignment_query.json.count | int > 0
        fail_msg: >-
          No role_user_assignment found for custom team role id {{ custom_team_role.id }} and user id {{ user.id }}.
      when: custom_team_role is defined and custom_team_role.id is defined

    - name: Assign Platform Auditor by Role User Assignments
      ansible.platform.role_user_assignment: &platform_auditor_assignment
        role_definition: Platform Auditor
        user: "{{ user3.id }}"
      register: platform_auditor_role_assignment

    - name: Assert that adding user as team admin worked
      ansible.builtin.assert:
        that:
          - platform_auditor_role_assignment is changed

    - name: Assign Platform Auditor by Role User Assignments check
      ansible.platform.role_user_assignment: *platform_auditor_assignment
      register: platform_auditor_role_assignment_check

    - name: Assert that platform_auditor_role_assignment_check is not changed
      ansible.builtin.assert:
        that:
          - platform_auditor_role_assignment_check is not changed

    - name: Delete Role User Assignments for Team Admin with object_ansible_id
      ansible.platform.role_user_assignment:
        state: absent
        object_ansible_id: "{{ team2_ansible_id }}"
        user: "{{ user4.id }}"
        role_definition: Team Admin
      register: delete_role_user_assignment_team3

    - name: Assert that removing user as team admin worked
      ansible.builtin.assert:
        that:
          - delete_role_user_assignment_team3 is changed

    - name: Check Existence of Role User Assignments for team2
      ansible.platform.role_user_assignment:
        state: exists
        object_ansible_id: "{{ team2_ansible_id }}"
        role_definition: Team Admin
        user: "{{ user4.id }}"
      register: role_definition_exists_check_team3
      ignore_errors: true

    - name: Assert that the role role_definition_exists_check_team3 is failed
      ansible.builtin.assert:
        that:
          - role_definition_exists_check_team3 is failed

    - name: Delete Role User Assignments for Organization Admin with object_ansible_id
      ansible.platform.role_user_assignment:
        state: absent
        object_ansible_id: "{{ org2_ansible_id }}"
        role_definition: Organization Admin
        user: "{{ user.id }}"
      register: delete_role_user_assignment_org

    - name: Assert that removing user as org admin worked
      ansible.builtin.assert:
        that:
          - delete_role_user_assignment_org is changed

    - name: Check Existence of Role User Assignments for org2
      ansible.platform.role_user_assignment:
        state: exists
        object_ansible_id: "{{ org2_ansible_id }}"
        role_definition: Organization Admin
        user: "{{ user.id }}"
      register: role_definition_exists_check_org2
      ignore_errors: true

    - name: Assert that the role role_definition_exists_check_org2 is failed
      ansible.builtin.assert:
        that:
          - role_definition_exists_check_org2 is failed

    - name: Delete Role User Assignments for Organization Admin
      ansible.platform.role_user_assignment:
        state: absent
        object_ids: ["{{ org.id }}", "{{ organization_name }}-2"]
        role_definition: Organization Admin
        user: "{{ user3.id }}"
      register: delete_role_user_assignment

    - name: Check Existence of Role User Assignments for orgs
      ansible.platform.role_user_assignment:
        state: exists
        object_ids: ["{{ org.id }}", "{{ organization_name }}-2"]
        role_definition: Organization Admin
        user: "{{ user3.id }}"
      register: role_definition_exists_check
      ignore_errors: true

    - name: Assert that the role role_definition_exists_check is failed
      ansible.builtin.assert:
        that:
          - role_definition_exists_check is failed

    - name: Assert that removing user as org admin worked
      ansible.builtin.assert:
        that:
          - delete_role_user_assignment is changed

    - name: Check Existence of Role User Assignments
      ansible.platform.role_user_assignment:
        object_ids: ["{{ org.id }}"]
        role_definition: Organization Admin
        user: "{{ user.id }}"

    - name: Check absence of Role User Assignments
      ansible.platform.role_user_assignment:
        object_ids: ["{{ org.id }}"]
        role_definition: Organization Member
        user: "{{ user.id }}"
        state: absent
      register: role_definition

    - name: Assert absence of Role User Assignments
      ansible.builtin.assert:
        that:
          - role_definition is not changed

    # </Role User Assignments> ------------------

  # # <Cleanup>
  always:
  # Always Cleanup
    - name: Remove custom team role user assignment if test created it
      ansible.platform.role_user_assignment:
        role_definition: "{{ custom_team_role_name }}"
        user: "{{ user.id }}"
        object_ids:
          - "{{ team1.id }}"
        state: absent
      when: custom_team_role is defined and custom_team_role.id is defined and team1 is defined
      register: custom_team_user_assignment_delete
      failed_when: >-
        custom_team_user_assignment_delete is failed and
        'Not found' not in custom_team_user_assignment_delete.msg | default('') and
        'does not exist' not in custom_team_user_assignment_delete.msg | default('')

    - name: Delete custom team role definition
      ansible.platform.role_definition:
        name: "{{ custom_team_role_name }}"
        content_type: "shared.team"
        permissions:
          - "shared.view_team"
        state: absent
      when: custom_team_role is defined and custom_team_role.id is defined
      register: custom_team_role_delete
      failed_when: >-
        custom_team_role_delete is failed and
        'Not found' not in custom_team_role_delete.msg | default('') and
        'does not exist' not in custom_team_role_delete.msg | default('')

    - name: Remove custom org role user assignment if test created it
      ansible.platform.role_user_assignment:
        role_definition: "{{ custom_role_name }}"
        user: "{{ user4.id }}"
        object_ids:
          - "{{ org.id }}"
        state: absent
      when: custom_role is defined
      register: custom_user_assignment_delete
      failed_when: >-
        custom_user_assignment_delete is failed and
        'Not found' not in custom_user_assignment_delete.msg | default('') and
        'does not exist' not in custom_user_assignment_delete.msg | default('')

    - name: Delete custom org role definition
      ansible.platform.role_definition:
        name: "{{ custom_role_name }}"
        content_type: "shared.organization"
        permissions:
          - "shared.view_organization"
        state: absent
      register: custom_role_delete
      failed_when: >-
        custom_role_delete is failed and
        'Not found' not in custom_role_delete.msg | default('') and
        'does not exist' not in custom_role_delete.msg | default('')

    - name: Delete users
      ansible.platform.user:
        username: "{{ item }}"
        state: absent
      when: "item in vars and 'id' in vars[item]"
      loop:
        - "{{ username }}--User-1"
        - "{{ username }}--User-2"
        - "{{ username }}--User-3"
        - "{{ username }}--User-4"

    - name: Delete Organizations
      ansible.platform.organization:
        name: "{{ item }}"
        state: absent
      when: "item in vars and 'id' in vars[item]"
      loop:
        - "org"
        - "{{ organization_name }}"
        - "{{ organization_name }}-2"

    - name: Delete all Teams
      ansible.platform.team:
        name: "{{ item }}"
        state: absent
      when: "item in vars and 'id' in vars[item]"
      loop:
        - "{{ name_prefix }}-Team-1"
        - "{{ name_prefix }}-Team-2"
...
